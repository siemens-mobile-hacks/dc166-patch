int _simi(unsigned first, char *buffer, unsigned count)
{
  return (int)(first + (unsigned)buffer + count);
}

int _simo(unsigned first, char *buffer, unsigned count)
{
  return (int)(first + (unsigned)buffer + count);
}

void argcv_break(void)
{
}

$EXTEND
$NOMOD166
$STDNAMES(reg.def)
$SEGMENTED
$CASE
$NOEXPANDREGBANK
$MODEL(LARGE)

        NAME DC166_SYSTEM

DC166_SYSTEM_PR SECTION CODE WORD PUBLIC 'CLIBRARY'
        PUBLIC _system

_system PROC FAR
        MOV R4,#0
        RETS
_system ENDP
DC166_SYSTEM_PR ENDS

        REGDEF R0-R15
        END
